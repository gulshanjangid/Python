#Encapsulation means keeping data and the methods that work on that data together inside a class, and controlling how that data can be accessed or changed.

# Think of it like an ATM:

# Your bank balance is internal data.
# You don't directly change the balance.
# You use methods like deposit() and withdraw().
# The methods control what is allowed.